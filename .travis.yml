language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t shikharsrivastava23/docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true shikharsrivastava23/docker-react npm run test
